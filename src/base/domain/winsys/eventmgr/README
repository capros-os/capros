This domain is the user input event multiplexor.  It receives events
from one or more device drivers, formats them and forwards them to 
the window system.  This used to exist as the DisplayMgr domain.
